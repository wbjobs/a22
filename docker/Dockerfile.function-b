FROM node:20-alpine

WORKDIR /app

COPY cmd/function-b/package*.json ./
RUN npm ci --only=production

COPY cmd/function-b/ .

EXPOSE 8083

HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
  CMD wget -qO- http://localhost:8083/health || exit 1

CMD ["node", "index.js"]
